{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"pygments_lexer":"ipython3","nbconvert_exporter":"python","version":"3.6.4","file_extension":".py","codemirror_mode":{"name":"ipython","version":3},"name":"python","mimetype":"text/x-python"},"kaggle":{"accelerator":"none","dataSources":[],"isInternetEnabled":false,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"#include <stdio.h>\n#include <stdlib.h>\n\nvoid printMonth(int month, int year);\nint getFirstDayOfMonth(int month, int year);\nvoid printCalendar(int month, int year, int firstDay);\n\nint main() {\n    int month, year;\n\n    // User input for month and year\n    printf(\"Enter month (1-12): \");\n    scanf(\"%d\", &month);\n    printf(\"Enter year: \");\n    scanf(\"%d\", &year);\n\n    printMonth(month, year);\n    \n    return 0;\n}\n\n// Function to print the month's name and year\nvoid printMonth(int month, int year) {\n    const char *months[] = {\n        \"January\", \"February\", \"March\", \"April\", \"May\", \"June\", \n        \"July\", \"August\", \"September\", \"October\", \"November\", \"December\"\n    };\n    \n    printf(\"\\n    %s %d\\n\", months[month - 1], year);\n    printf(\"Su Mo Tu We Th Fr Sa\\n\");\n\n    // Get the first day of the month\n    int firstDay = getFirstDayOfMonth(month, year);\n    \n    // Print the calendar\n    printCalendar(month, year, firstDay);\n}\n\n// Function to calculate the first day of the month (0 = Sunday, 1 = Monday, ..., 6 = Saturday)\nint getFirstDayOfMonth(int month, int year) {\n    // Zeller's Congruence Algorithm to calculate the first day of the month\n    if (month < 3) {\n        month += 12;\n        year--;\n    }\n\n    int k = year % 100;\n    int j = year / 100;\n    \n    int firstDay = (1 + (13 * (month + 1)) / 5 + k + k / 4 + j / 4 + 5 * j) % 7;\n    return firstDay;\n}\n\n// Function to print the calendar for the given month and year\nvoid printCalendar(int month, int year, int firstDay) {\n    // Number of days in each month\n    int daysInMonth[] = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};\n    \n    // Adjust for leap year if month is February\n    if (month == 2 && ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0))) {\n        daysInMonth[1] = 29;\n    }\n\n    int days = daysInMonth[month - 1];\n    \n    // Print leading spaces for the first day of the month\n    for (int i = 0; i < firstDay; i++) {\n        printf(\"   \");\n    }\n    \n    // Print days of the month\n    for (int day = 1; day <= days; day++) {\n        printf(\"%2d \", day);\n        \n        // Start a new line after Saturday\n        if ((firstDay + day) % 7 == 0) {\n            printf(\"\\n\");\n        }\n    }\n    \n    printf(\"\\n\");\n}\n","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true},"outputs":[],"execution_count":null}]}